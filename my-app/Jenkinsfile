pipeline {
    agent any

    environment {
        SONARQUBE_URL = "http://3.235.142.37:9000"  // Your SonarQube Server
    }

    stages {
        stage("Build") {
            steps {
                dir("my-app") {
                    sh "mvn clean package"
                }
            }
        }

        stage("SonarQube Analysis") {
            steps {
                dir("my-app") {
                    sh "sonar-scanner"
                }
            }
        }
    }

    post {
        success {
            echo "Pipeline completed successfully!"
        }
        failure {
            echo "Pipeline failed. Check logs for details."
        }
    }
}
